meta {
  name: Confirm Download
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/broker/api/v1/filetransfer/{{fileTransferId}}/confirmdownload
  body: none
  auth: bearer
}

auth:bearer {
  token: {{recipient_altinn_token}}
}

tests {
  test("Status code is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("Response is empty", function() {
    expect(res.getBody()).to.equal('');
  });
}
