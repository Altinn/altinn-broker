meta {
  name: Delete subscription
  type: http
  seq: 2
}

delete {
  url: {{platform_base_url}}/events/api/v1/subscriptions/{{subscription_id}}
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}
