meta {
  name: Create subscription
  type: http
  seq: 1
}

post {
  url: {{platform_base_url}}/events/api/v1/subscriptions
  body: json
  auth: inherit
}

body:json {
  {
      "endpoint": "You can create a test endpoint at https://webhook.site",
      "resourceFilter": "urn:altinn:resource:{{resource_id}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
