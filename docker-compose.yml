version: '3.4'

volumes:
    broker-storage:

networks:
  altinnplatform_network:
    external: false

services:
  azurite:
    image: mcr.microsoft.com/azure-storage/azurite
    ports:
      - "10000:10000"
      - "10001:10001"
  database:
    image: 'postgres:latest'
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: broker
  broker-flyway-local:
    image: flyway/flyway@sha256:18a901187f2d5cac5e0d426e6adf656d47342661ae181cd69e6e983f00a36372
    command: -url='jdbc:postgresql://database:5432/broker' -user=postgres -password=postgres -connectRetries=60 migrate -validateMigrationNaming='true'
    volumes:
      - ./src/Altinn.Broker.Persistence/Migrations:/flyway/sql
    depends_on:
      - database
    entrypoint:
      [
        "flyway",
      ]
