# This file will override the default PostgreSQL configuration
max_prepared_transactions = 64

# Include all other settings from the default postgresql.conf
include_if_exists = '/usr/share/postgresql/postgresql.conf.sample'